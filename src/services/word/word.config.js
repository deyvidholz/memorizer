export const memorizerConfig = {
  key: 'memorizer', // key in localStorage
  maxCountingToMemorize: 10, // how many times user must get the right translation before set as memorized
  defaultData: { words: {} }, // default memorizer data object
};
